26 하계 모각소 3차 모임 기록

일시 - 2026.07.22

개인주제 - 안드로이드 보안 기술 우회 개념 및 실습(루팅 탐지 우회)

# 안드로이드 보안 기술 우회

## **1. 개요**

모바일 앱 보안 솔루션의 목적은 공격자가 앱을 분석하기 어렵게 만드는 것이다. 대표적인 방어 기법은 다음과 같다.

- 루팅 탐지
- 안티 디버깅
- 앱 무결성 검증
- 코드 난독화

아래에서는 각 기법의 원리와 우회 방법을 정리한다.

## **2. 루팅 탐지**

루팅된 기기에서 나타나는 특징을 체크해 루팅 여부를 판단하는 기법이다. 주요 탐지 방식은 다음과 같다.

- 루팅 패키지 체크: 루팅한 기기에 설치되는 앱 패키지를 확인
- 파일 체크: 루팅한 기기에만 존재하는 파일을 확인
- build.prop 체크: 루팅 시 변경될 수 있는 build.prop 값을 확인
- 디렉토리 권한 체크: 루팅 후 시스템 디렉토리 권한이 변경되었는지 확인

### **우회 방법**

**코드 패치**

바이너리나 smali 코드를 변조한 뒤 재패키징해 탐지 로직 자체를 수정하는 방법이다.

**함수 후킹**

탐지 로직 함수를 후킹해 인자값이나 리턴값을 변조하는 방법이다.

## **3. 앱 무결성 검증**

앱 바이너리와 리소스가 변조되었는지 확인하는 기법이다. 코드 패치한 앱의 사용을 막고 악의적 유포를 방지하기 위해 사용한다.

검증 방식은 두 가지다.

- 파일 hash값 비교
- dex 파일을 암호화해 저장한 뒤 앱 실행 시 복호화해 로딩

무결성 검증은 클라이언트 내부에서 자체적으로 hash값을 검증하는 방식과 서버를 통해 검증하는 방식으로 나뉜다.

### **검증 위치별 우회 포인트**

**서버 측**

통신 데이터를 변조해 앱이 서버로 전달하는 hash값이나 서버가 반환하는 검증 결과를 조작한다.

**클라이언트 측**

함수 후킹으로 검증 로직을 우회한다. 동적 로딩 과정에서 변조된 dex 파일로 교체되도록 로딩 함수를 후킹하는 방식도 있다.

## **4. 안티 디버깅**

디버거가 보호 대상 프로세스에 attach하는 것을 막는 기법이다. 디버거의 존재를 확인해 해당 프로세스가 정상 동작하지 못하도록 막는다.

### **탐지 방식**

**시간 기반 탐지**

두 명령어 사이의 실행 시간이 일정 기준을 넘으면 디버깅 중인 것으로 판단하는 전통적인 방법이다.

**파일 기반 탐지**

다음 파일을 확인해 디버깅 여부를 판단한다.

- `/proc/pid/status`, `/proc/pid/task/pid/status`의 TracerPid 값
- `/proc/pid/stat`, `/proc/pid/task/pid/stat`: 디버깅 중이면 괄호 뒤 첫 문자가 t
- `/proc/pid/wchan`, `/proc/pid/task/pid/wchan`: 디버깅 중이면 ptrace_stop

**VM 필드 기반 탐지**

안드로이드 자바 VM 구조체 값을 통해 탐지한다.

**ptrace 기반 안티 디버깅**

하나의 프로세스에는 하나의 디버거만 attach할 수 있다는 점을 이용해 앱이 스스로 ptrace를 선점하는 방법이다.

**BP Scan**

디버깅 시 설정되는 브레이크포인트를 탐지하는 방법이다.

### **우회 방법**

- 코드 패치: 바이너리나 smali 코드를 변조한 뒤 재패키징
- 함수 후킹: 안티 디버깅 함수를 후킹해 탐지를 우회

## **5. SSL Pinning**

SSL/TLS 통신 시 사용하는 인증서를 앱 서버의 인증서로 고정하는 기법이다. MITM 공격을 방지하기 위해 사용한다. Frida를 이용하면 우회할 수 있다.

## **6. 실습 1: UnCrackable Level 1 루팅 탐지 우회**

### **패키지명 및 진입점 식별**

분석의 시작은 대상을 정확히 파악하는 것이다. JADX-GUI에 APK 파일을 드래그 앤 드롭해 분석을 시작한다.

- 패키지명 확인: `AndroidManifest.xml` 최상단 `<manifest package="...">` 태그에서 앱 고유 패키지 이름을 확인
- 진입점 확인: `<intent-filter>` 내에 `android.intent.action.MAIN`이 설정된 액티비티를 찾음

실습 결과 `sg.vantagepoint.uncrackable1.MainActivity`가 진입점임을 확인했다.

### **루팅 탐지 로직 추적**

`MainActivity`의 `onCreate` 메서드를 집중적으로 분석했다.

- `onCreate` 내부에서 "Root detected" 메시지를 띄우는 조건문을 확인
- `if (c.a() || c.b() || c.c())` 형태로, 클래스 `c`의 메서드 `a`, `b`, `c` 중 하나라도 참이면 탐지된 것으로 판단하는 로직을 발견
- 해당 메서드를 더블 클릭해 정의부로 이동한 뒤 전체 클래스 경로 `sg.vantagepoint.a.c`를 확보

### **Frida 스크립트 작성 (Spawn 및 후킹)**

앱이 실행되자마자 종료되므로 프로세스가 완전히 뜨기 전에 개입하는 Spawn 방식을 사용한다.

- `Java.perform` 내부에서 `Java.use("sg.vantagepoint.a.c")`로 탐지 클래스를 로드
- 메서드 `a`를 후킹해 implementation을 재정의
- 로그로 함수 호출 여부를 확인 (`send("Root check function called")` 등)
- 리턴값을 `return false;`로 강제해 탐지되지 않은 것처럼 조작

### **스크립트 실행 및 결과 검증**

작성한 `bypass.js`를 파이썬 인젝터로 실행한다.

```
python injector.py
```

검증 순서는 다음과 같다.

1. 터미널에 "JS Loaded"와 후킹 메시지가 출력되는지 확인
2. 앱 화면에서 "Root Detected" 경고창이 더 이상 뜨지 않거나, 확인을 눌러도 앱이 종료되지 않고 정상 진입되는지 확인
3. 필요시 `b`, `c` 메서드도 동일하게 후킹해 모두 false를 리턴하도록 처리

## **7. 실습 2: AndroGoat 루팅 탐지 우회**

### **대상 및 상황 분석**

- 대상 앱: AndroGoat
- 증상: 앱 실행은 정상이나 Root Detection 메뉴에서 Check Root 버튼을 누르면 "Device is Rooted" 토스트 메시지가 출력됨
- 목표: 루팅된 폰에서도 "Device is not Rooted"가 뜨도록 우회

### **패키지명 확인 및 연결 테스트**

- JADX로 APK를 열고 `AndroidManifest.xml`의 `<manifest package="...">` 태그에서 패키지명 확인: `owasp.sat.agoat`
- `console.log("JS Loaded")`가 포함된 기본 스크립트를 실행해 앱과 정상적으로 연결되는지 테스트

### **문자열 검색을 통한 로직 추적**

앱 실행 시점에 바로 탐지가 일어나지 않으므로 진입점(`onCreate`)부터 찾는 방식은 비효율적이다. 화면에 출력된 메시지를 단서로 역추적한다.

- 단서: 버튼 클릭 시 화면에 뜬 문구 "Device is Rooted"
- JADX의 Text Search 기능으로 해당 문자열 검색
- 검색 결과 `RootDetectionActivity` 클래스에서 해당 문자열을 발견
- `if (isRooted())` 조건문이 참일 때 해당 메시지를 띄우는 구조를 확인 → `isRooted` 메서드가 탐지 로직의 핵심

### **Frida 후킹 스크립트 작성 및 검증**

- 클래스: `owasp.sat.agoat.RootDetectionActivity`
- 메서드: `isRooted`
- 스크립트 실행 후 버튼을 눌렀을 때 로그가 찍히고 원래 리턴값(true)이 확인됨

### **리턴값 변조를 통한 우회**

탐지 로직 후킹이 정상 동작함을 확인했으므로 리턴값을 뒤집는다.

- `isRooted` 메서드의 implementation에서 `return false;`로 고정
- 최종 결과: 앱에서 버튼을 다시 누르면 "Device is not Rooted" 문구가 출력되며 우회 성공
