26 하계 모각소 2차 모임 기록

일시 - 2026.07.16

개인주제 - 버그헌팅 타겟 정보 수집 방법과 앱 취약점 등 애플리케이션 보안 이해

# 1. 정보 수집(Reconnaissance) 방법

## **1.1 정보 수집의 필요성**

정보 수집이란 취약점을 찾을 대상에 대한 정보를 모으는 과정이다. 내가 활용할 수 있는 자원과 타겟에 대해 많이 알수록 발견 가능한 취약점의 폭이 넓어진다.

효과적으로 취약점을 찾아내려면 사전 정보 수집이 필수다. 대상 사이트/기업이 어떤 서버 기술을 쓰고, 인프라가 어디서 동작하는지 알아야 짧은 시간 안에 취약점을 찾아낼 수 있다.

## **1.2 수동 탐색 (Manual Exploration)**

정보 수집의 가장 기본적인 첫 단계다. 애플리케이션을 자동화 도구로 분석하기 전에 직접 사용해보는 것이 중요하다.

- 어떤 동작이 가능하고 어떤 기능이 존재하는지 확인
- 모든 페이지에 들어가보고 모든 기능을 직접 사용해보기
- 낯선 용어는 그때그때 공부하면서 진행
- 각 기능별로 공격 가능성을 정리 (예: "선물하기" 기능이 있다면 → 선물할 제품/수량/가격을 클라이언트 단에서 조작할 수 있을까?)

## **1.3 구글 해킹 (Google Dorking)**

구글링을 통해 원하는 정보를 효율적으로 찾는 기법. 구글 검색 연산자를 활용한다.

| **연산자** | **설명** | **예시** |
| --- | --- | --- |
| `site:` | 특정 사이트 내 결과만 검색 | `site:youtube.com` |
| `inurl:` | URL에 특정 문자열 포함 | `inurl:admin` |
| `intitle:` | 페이지 Title에 특정 문자열 포함 (디렉토리 리스팅 취약점 탐색 시 자주 사용) | `intitle:"index of"` |
| `link:` | 특정 링크를 포함한 페이지 검색 (숨겨진 페이지, 테스트 페이지, 크리덴셜 노출 페이지 발견 가능) | `link:https://...` |
| `filetype:` / `ext:` | 특정 확장자 파일만 검색 (타겟이 자주 쓰는 문서 유형 파악) | `filetype:pdf` |
| `*` (와일드카드) | 임의의 문자열을 의미 | `admin * login` |
| `" "` (큰따옴표) | 공백 포함 정확한 문구로 검색 | `"gk gk"` |
| `-` | 특정 단어 제외 | `admin -test` |

**활용 예시**

- `site:*.targetSystem.com` → 서브도메인까지 포함해 삭제되지 않았거나 관리되지 않는(방치된) 페이지 발견 가능
- `site:*.targetSystem.com inurl:admin`
- `site:*.targetSystem.com inurl:app/kibana`
- `site:s3.amazonaws.com TargetName` → S3 버킷 탐색
- `site:*.targetSystem.com ext:txt password` → 특정 사이트 내 민감 키워드 포함 파일 검색

**LFI(파일 경로 조작) 흔적 검색**

- `inurl:/etc/passwd "root:x:0:0:root:/root:/bin/bash"` → `/etc/passwd`가 그대로 노출된 페이지 탐색
- `domain.com/?page=/etc/passwd` 형태의 파라미터 구조를 가진 페이지를 구글 검색으로 유추 가능

**디렉토리 리스팅 취약점**

- `intitle:"index of"` — 웹 서버가 디렉토리 목록을 그대로 노출하는 페이지 탐색

**참고 자료**

- Google Hacking Database (GHDB) — 구글 해킹 쿼리를 모아둔 exploit-db의 데이터베이스

## **1.4 Spidering**

사이트 내에서 접근 가능한 모든 페이지/엔드포인트를 자동으로 식별하는 기법.

**대표 도구**

- **ZAP (OWASP ZAP)**: 웹 프록시 기반으로 Burp Suite와 유사한 동작. 도메인 주소를 입력하면 사이트를 크롤링하며 존재하는 페이지들을 자동 수집
    - ZAP 공식 다운로드
- **Burp Suite**: Spider/Crawler 기능으로 동일한 목적 수행 가능

## **1.5 S3 Bucket 정찰**

AWS가 제공하는 오브젝트 스토리지 서비스. 접근 제어(권한 설정)가 잘못된 버킷에서 숨겨진 서버 정보, 로그, 크리덴셜, 사용자 정보, 소스코드 등 민감 정보가 노출되는 경우가 많다.

- `site:s3.amazonaws.com [Target]`
- `site:amazonaws.com [Target]`

**대표 도구**

- **GrayhatWarfare**: 공개 S3 버킷 및 오브젝트를 검색해주는 웹 서비스
- **BucketStream**: 인증서 투명성 로그(Certificate Transparency, CT)를 모니터링해 새로 생성되는 도메인명으로부터 연관된 S3 버킷 이름을 추정/탐색하는 도구

## **1.6 GitHub Recon**

타겟 기업의 GitHub Repository를 조사하면 민감 정보나 취약점 단서를 얻을 수 있다. 개발 과정에서 테스트용 파일, 백업 파일, 설정 파일 등이 실수로 커밋되는 경우가 많기 때문이다.

**조사 방법**

- 타겟 회사명, 프로젝트명, 개발자명으로 검색
- Issues, Commit 이력 조사
- Code History 및 `git blame` 내역 조사 (과거 커밋에서 삭제된 민감 정보가 히스토리에 남아있는 경우가 많음)

**발견 가능한 정보**

- API Key, 암호화 Key, DB 비밀번호 등
- 검색 키워드 예시: `key`, `secret`, `password`, `cred`, `token`

**대표 도구**

- **truffleHog**: Git 히스토리 전체를 스캔해 정규식/엔트로피 기반으로 시크릿 탐지
- **Gitrob** (현재 유지보수 중단, 후속으로 **GitLeaks**, **Gitrob 포크 버전** 등 사용 권장): 조직/레포 내 민감 파일, 시크릿 탐지

## **1.7 Domain / Subdomain Enumeration**

타겟 도메인의 서브도메인을 조사해 공격 표면(Attack Surface)을 넓히는 작업.

- **인증서 SAN(Subject Alternative Name) 조사**: 하나의 TLS 인증서에는 여러 도메인이 SAN으로 등록되는 경우가 많아, 인증서 정보를 통해 연관 서브도메인을 파악할 수 있음
- **crt.sh**: 인증서 투명성(CT) 로그를 검색해 발급된 인증서 기반으로 서브도메인을 확인할 수 있는 사이트
- **Amass**: OWASP에서 제공하는 서브도메인 열거 도구. DNS, CT 로그, 검색엔진 등 다양한 소스를 종합해 서브도메인 탐색
- 그 외: Sublist3r, Subfinder, assetfinder 등도 병행 활용 가능

---

# 2. 애플리케이션 보안의 이해 — 모바일 앱 해킹

## **2.1 모바일 앱 공격 분류**

- **모바일 앱 자체 공격**: 앱의 흐름을 조작해 악용 (예: 게임 재화 조작, 로그인 흐름 우회, 개인정보 유출)
- **모바일 앱 서버 공격**: 앱이 통신하는 백엔드 서버의 취약점을 악용 (예: SQL Injection, 파라미터 변조)

## **2.2 모바일 앱 유형**

| **유형** | **설명** |
| --- | --- |
| Web App | 웹사이트를 그대로 앱 형태로 감싼 것 |
| Hybrid App | 네이티브 요소와 웹뷰(HTTP 기반 콘텐츠)가 혼재 |
| Native App | 플랫폼 고유 언어/API로 직접 개발되어 OS와 직접 통신 |

## **2.3 취약점 분석 환경 구축**

### **Rooting / Jailbreak**

단말기에서 최고 관리자 권한(root)을 획득하는 과정. 필요한 이유:

- 단말기에서 실행 중인 앱 프로세스에 접근해 코드 흐름을 조작하기 위해
- 앱이 사용하는 데이터를 열람 및 수정하기 위해

### **Android Rooting 절차**

1. **Bootloader Unlock**
    - Bootloader: 부팅 초기에 실행되며 커널이 정상 로드되도록 무결성 등을 점검하고 RAM에 커널을 로드하는 역할
    - Bootloader Locked: 사용자가 임의로 펌웨어를 수정하지 못하도록 제한된 상태 (기본값)
    - Bootloader Unlocked: Custom Kernel, Custom Recovery 등을 설치하기 위한 선행 조건
2. **Custom Recovery 설치**
    - Recovery: 백업, 초기화 등 OS 유지보수 기능을 제공하는 모드. 순정(Stock) Recovery는 최소 기능만 제공
    - Custom Recovery: AOSP 기반 Recovery에 기능을 추가해 제한을 우회한 버전 (예: TWRP)
    - 설치 방법: `fastboot` 모드 이용, fastboot가 지원되지 않는 기기는 Odin(삼성 계열) 등 벤더 툴 이용
3. **Custom ROM 설치**
    - ROM: 안드로이드 기기 펌웨어
    - Custom ROM: 순정 ROM 이미지에 `setuid` 권한을 가진 `su` 바이너리를 추가한 버전
    - Custom Recovery를 통해 Custom ROM(루팅된 안드로이드)을 플래시

> 흐름 요약: **Bootloader Unlock → Custom Recovery 설치 → Custom ROM 플래시(su 바이너리 포함) → Root 권한 획득**
> 

### **iOS Jailbreak**

iOS는 root 권한을 얻기 위해 해당 iOS 버전의 커널/시스템 취약점이 발견되어야 한다.

| **유형** | **설명** |
| --- | --- |
| Untethered Jailbreak | 완전 탈옥. 재부팅해도 탈옥 상태 유지 |
| Tethered Jailbreak | 반탈옥. 재부팅 시 PC 연결 후 재탈옥 필요, 없으면 부팅 불가 |
| Semi-tethered Jailbreak | 준반탈옥. 재부팅 시 일반 iOS로는 부팅되나, 탈옥 상태로 쓰려면 PC 또는 앱으로 재실행 필요 |
- 탈옥 방법 예: **3uTools** 등 탈옥 툴 이용
- **iOS Tweak**: 탈옥된 iOS에서 사용하는 앱/모듈로, **Cydia**, **Sileo** 같은 패키지 매니저를 통해 설치

### **Proxy 세팅 (트래픽 가로채기)**

**Android**

- Wi-Fi 프록시 수동 설정 (분석 PC와 같은 네트워크에 연결되어 있어야 함)
- HTTPS 트래픽 분석을 위해 Burp Suite 등의 CA 인증서 설치 필요:
    1. Burp에서 Proxy Listener 오픈 및 프록시 설정
    2. Burp CA 인증서 다운로드
    3. 인증서 확장자를 `.cer`로 변경
    4. 설정 > 보안 > SD카드에서 설치 (사용자 인증서로 설치됨)
    5. Android 7 이상은 사용자 인증서를 기본 신뢰하지 않으므로, 루팅 후 `/system` 디렉토리에 시스템 인증서로 이식(mount & 인증서 이동, 쓰기 권한 부여) 필요 — 또는 Frida/Magisk 모듈(예: Magisk `TrustUserCerts`)로 우회 가능

**iOS**

- 설정 > Wi-Fi > 프록시 수동 설정 (서버 주소, 포트 입력)
- Burp가 열려있는 상태에서 `http://<burp-ip>:<port>/` 접속 후 CA 인증서 다운로드
- 설정 > 일반 > VPN 및 기기 관리에서 인증서 설치
- 설정 > 일반 > 정보 > 인증서 신뢰 설정에서 해당 CA를 **완전 신뢰(Enable Full Trust)** 로 활성화해야 실제로 트래픽 복호화가 가능

### **Root Shell 접속**

- **Android**: ADB(Android Debug Bridge) 이용
    - 실기기: `adb shell`
    - 에뮬레이터: `adb connect 127.0.0.1 <port>` 후 `adb shell`
- **iOS**: SSH 이용
    - 3uTools 등에서 SSH 활성화 기능 제공, 탈옥 후 OpenSSH 설치 시 SSH 접속 가능

## **2.4 Android App 구조 이해**

**실행 방식 변천**

- **JVM**: 자바 프로그램 실행 가상머신. 바이트코드를 JVM에서 실행
- **Dalvik VM**: 라이선스 이슈(Oracle Java) 회피를 위해 안드로이드가 자체 개발한 VM. 바이트코드를 실행 시점에 **JIT(Just-In-Time) 컴파일**하여 네이티브 코드로 변환하며 실행
- **ART (Android Runtime)**: 앱 설치 시점에 **AOT(Ahead-Of-Time) 컴파일**을 수행해 `.oat` 파일(완전한 네이티브 코드)로 미리 변환 → 실행 속도 향상 (Dalvik 이후 도입, Android 5.0부터 기본 런타임)

**Android 관련 언어/포맷**

- **DEX (Dalvik Executable)**: 안드로이드 앱의 실행 파일 포맷
- **Smali**: Dalvik 바이트코드를 사람이 읽을 수 있게 표현한 어셈블리 유사 언어 (리버싱/패치 시 사용)

**앱 위변조 방지 — 코드 서명**

- 앱 개발자가 자신의 코드 서명 인증서로 APK에 서명하는 것
- 목적: 공격자가 악의적으로 패치한 앱을 원 개발자 명의로 재배포하는 것을 막기 위함 (서명이 다르면 같은 패키지명으로 업데이트 설치 불가)

**AndroidManifest.xml**

앱의 메타정보를 담은 필수 설정 파일:

- 고유 패키지 이름(Package ID)
- Activity 등 컴포넌트(화면) 정의
- 권한(Permission) 정의
- 외부 라이브러리 및 `exported` 속성 등 컴포넌트 노출 여부

**Activity**

- 앱 실행 시 사용자에게 보여지는 화면 단위, 사용자와 상호작용하는 UI 제공
- 하나의 클래스로 구현되며 생명주기(Lifecycle) 콜백을 가짐:`onCreate()` → `onStart()` → `onResume()` (화면 활성화)→ 다른 화면으로 전환 시 `onPause()` → `onStop()`→ 액티비티 종료 시 `onDestroy()`

**Broadcast Receiver**

- 안드로이드 시스템 또는 다른 앱에서 브로드캐스트(방송)되는 인텐트를 수신하는 컴포넌트

## **2.5 Android App 주요 취약점**

**정보 유출(Data Leak) 유형**

- **Logging**: `logcat`을 통해 민감 데이터가 로그에 그대로 남아 유출
- **하드코딩(Hardcoding)**: 소스코드 내 크리덴셜(ID/PW, 키 등)이 평문으로 하드코딩
- **Data Storage**: 디바이스 로컬 저장소(SharedPreferences, SQLite, 파일 등)에 크리덴셜이 평문으로 저장
- **API Key 노출**: 소스코드 또는 리소스 파일(strings.xml 등)에 API Key가 그대로 노출
- **API Key 오남용 확인**: 유출된 키가 실제로 악용 가능한 권한을 가지는지(예: 과금, 데이터 접근) 검증하는 절차

**AWS Cognito 취약점**

- AWS Cognito는 로그인/인증·인가를 손쉽게 구현할 수 있게 해주는 서비스 (Identity Pool, User Pool)
- 소스코드, 리소스 파일, 또는 앱-서버 간 HTTP 트래픽에서 **Identity Pool ID**가 노출되는 경우가 많음
- 노출된 Identity Pool ID로 AWS STS를 통해 **임시 자격증명(Temporary Credential)** 을 발급받아 해당 Identity Pool에 연결된 AWS 리소스(S3 등)에 접근 가능한지 확인

**딥 링크(Deep Link) 취약점**

- 특정 URI 클릭 시 앱의 특정 Activity가 실행되는 기능
- `AndroidManifest.xml`의 `<intent-filter>` 내 `scheme`, `host` 등으로 정의됨
- **피싱 리다이렉션**: 딥 링크로 전달된 URL 파라미터를 검증 없이 WebView로 그대로 로드할 경우 발생
    - 예: `insecure://shop?url=http://hacker.com/login_phishing` → 정상 앱 내에서 공격자의 가짜 로그인 페이지가 렌더링되어 계정 정보 탈취 가능
- **호스트 검증 우회**: `endsWith()` 등 느슨한 검증 로직을 사용할 경우, `http://hacker.com?dummy=target.com` 같은 방식으로 파라미터를 조작해 필터를 우회 가능

**Broadcast Receiver 취약점**

- 안드로이드 시스템/다른 앱이 보내는 브로드캐스트를 수신하는 컴포넌트에서 발생
- **악성 WebView 실행**: `exported=true`로 설정된 Receiver가 수신 인텐트의 URL을 검증 없이 WebView로 띄우면, 공격자가 임의의 브로드캐스트를 전송해 피해자 화면에 악성 사이트를 띄울 수 있음
- **암시적 인텐트(Implicit Intent) 정보 유출**: 앱이 수신 대상을 명시하지 않고(암시적 인텐트) 민감 정보(ID/PW 등)를 브로드캐스트로 전송하면, 공격자가 제작한 악성 앱이 동일한 인텐트 필터를 등록해 이를 가로채(Sniffing) 정보를 탈취할 수 있음

**인텐트 리다이렉션(Intent Redirection) / Content Provider**

- **인텐트 리다이렉션**: 외부에서 직접 접근 불가능한(`exported=false`) 비공개 Activity를, 외부 접근 가능한 Proxy Activity를 경유해 우회 실행하는 기법. Extra 데이터에 실행할 인텐트를 중첩해서 담는 방식(Nested Intent)을 사용
- **Content Provider**: 앱 간 데이터 공유를 위한 컴포넌트. `exported=true`로 잘못 설정되고 권한(permission) 검증이 없으면, 공격자가 앱의 내부 DB나 파일에 무단으로 접근해 데이터를 탈취 가능

## **2.6 iOS 주요 취약점**

**① 크리덴셜 하드코딩**

- 안드로이드와 동일하게 소스코드/리소스 파일 내 개발 서버 주소, API Key, 인증서, 계정 정보 등이 평문으로 저장되는 경우
- 분석 방법: `.plist` 파일 점검, 바이너리는 **IDA Pro**, **Ghidra**, **Hopper** 등 디컴파일 도구로 문자열(String) 분석 수행

**② URL Scheme**

- 특정 URL로 앱을 실행시키는 기능 (Android Deep Link와 유사한 개념)
- 공격 시나리오: 앱이 `openURL` 등으로 외부에서 전달된 URL을 검증 없이 처리할 경우, 공격자가 만든 악성 링크를 실행하도록 유도 가능
- 위험성: 사용자가 의도치 않게 피싱 사이트로 이동하거나, 공격자가 원하는 특정 기능이 강제로 실행될 수 있음

**③ UIWebView / 자바스크립트 관련 취약점**

- `UIWebView`는 Apple이 iOS 12부터 **Deprecated**(사용 중단 권고, 이후 App Store 신규 제출 시 사용 자체가 제한)로 지정한 구형 웹뷰 컴포넌트로, `WKWebView`에 비해 보안 패치가 부족해 스크립트 처리 관련 취약점(XSS 등)에 노출될 위험이 높음
- 점검 포인트: 정적 분석 시 앱이 최신 `WKWebView`가 아닌 구형 `UIWebView`를 사용하는지 확인하고, 로드되는 페이지에서 임의 스크립트 실행이 가능한지 점검

**④ 클라이언트 측 인증/인가 로직**

- 로그인이나 권한 검증 같은 중요 절차를 서버가 아닌 앱(클라이언트) 내부 로직으로 처리할 때 발생
- 근본 원인: 단말기 소유자는 Jailbreak를 통해 root 권한을 가질 수 있으므로, 앱 내부의 실행 흐름을 임의로 통제할 수 있음
- 공격 예시: 클라이언트 코드에 `if (inputPw == correctPw) { loginSuccess() }` 같은 검증 로직이 존재할 경우, Frida 등으로 비교 결과나 분기(branch)를 후킹해 강제로 로그인 성공 흐름으로 우회 가능
- 대응 방안: 인증(Authentication)/인가(Authorization) 판단은 반드시 서버 측에서 최종 검증해야 함

## **2.7 앱 로직 조작 — 함수 후킹(Function Hooking)**

**원리**

후킹은 코드의 실행 흐름을 중간에서 가로채는 기술이다. 특정 함수가 실행되기 직전(`onEnter`) 또는 실행된 직후(`onLeave`)에 공격자가 작성한 임의 코드를 삽입해 실행한다.

**실행 흐름**

```
[함수 호출 지점] → [Hook: onEnter — 인자 확인/변조 가능] → [원본 함수 실행] → [Hook: onLeave — 리턴값 확인/변조 가능] → [호출부로 결과 반환]
```

후킹은 함수 실행 자체를 막거나, 다른 코드로 대체 실행한 뒤 원래 흐름으로 복귀시키는 등 자유로운 흐름 제어가 가능하다. (대표 도구: **Frida** — `Interceptor.attach()`의 `onEnter`/`onLeave` 콜백)
