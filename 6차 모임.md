4주차에 제출한 레포트는 예비 검토 통과 후 아직 보안팀 검토중이라는 답변을 받았다. 

이번 6주차는 지난 4주차와 5주차에서 분석하던 것과 같은 APK를 이어서 진행했다. 앞선 주차에서 잡아둔 진입 우회 환경을 그대로 이어받아 남은 공격면을 검증하는 데 집중했다.

## **목표**

지난주와 마찬가지로 한 대형 안드로이드 벤더의 앱을 대상으로 실제 임팩트로 이어지는 취약점을 찾는 것이 목표였다. 이번에는 컴포넌트가 외부에 열려 있다는 사실만이 아니라 외부 입력이 내부 민감 지점까지 도달하여 유효한 취약점을 발견하는 것이 목표였다. 

## **진행 과정**

1. 지난주와 마찬가지로 같은 APK에서 분석 환경을 이어받았다. 앞선 주차에서 만든 표준 에뮬레이터 환경과 후킹 스크립트를 그대로 사용했다.
2. 실행 장애 우회를 재확인했다. 앱이 벤더 전용 클래스에 의존해 일반 환경에서 초기화 단계에 문제가 났다. 조기 후킹으로 초기화 직전 경계를 잡아 벤더 종속 검사를 건너뛰고 주요 화면까지 진입시켰다.
3. 초기 동의 화면과 기능 활성화 검사를 우회했다. 일반 환경에는 벤더 전용 설정 항목이 없어서 정상 경로로는 못 넘어갔다. 검사 메서드를 후킹해 대상 화면까지 도달했다.
4. 내부 라우팅을 전수 분석했다. 진입점 컴포넌트가 dispatcher 역할을 했다. 관련 경로 여러 개를 실제로 실행해 동작을 확인했다.
5. 공격면 후보를 하나씩 검증하고 판정했다. 외부 입력이 처리되는 경로 일부는 동적으로 확인됐다. 다만 실제 호출 가능한 기능은 무해한 UI 상태 처리뿐이었다.
6. 오탐이 되기 쉬운 경로를 걸러냈다. 위험해 보이는 sink는 존재했지만 입력 소스가 서버 제공 데이터라서 외부 공격자가 제어할 수 없었다. 식별자 입력값도 통계용으로만 쓰여 IDOR로 이어지지 않았다. 압축 처리 코드도 공격자 제어 소스와 연결되지 않았다.
7. 식별자 유출 후보는 실기기 조건 문제로 판단했다. 값이 외부로 나가는 흐름은 확인됐지만 테스트 환경에서는 비어 있거나 빈 결과로 나왔다. 합성 데이터를 진짜 유출처럼 꾸미는 것은 금지라서 결국 가능성만 열어두고 제출은 하지 않았다.

## **사용 도구**

지난주와 동일하게 정적 분석 도구, 표준 에뮬레이터, 디바이스 제어 도구, Frida, 요청 확인용 외부 엔드포인트를 사용했다.

## **성과**

직접 제출 가능한 새 취약점은 확정하지 못했다. 대신 분석적으로 넓은 범위를 정리했다.

동적으로 확인한 것은 다음과 같다. 일반 환경에서도 벤더 종속 검사를 우회해 앱을 구동할 수 있었다. 외부 입력이 일부 내부 경로까지 전달됐다. 외부에서 접근 가능한 인터페이스 객체가 존재했다.

정적으로 확인한 것은 다음과 같다. 앱은 패킹되지 않았다. 현재 확인된 인터페이스에는 민감 기능이 없었다. 파일 토큰 계정 내부 컴포넌트로 이어지는 임팩트는 입증되지 않았다. 위험 후보의 입력 소스는 서버 데이터였다.

## **회고**

이번 앱은 임팩트 확정에 벤더 실기기가 필요한 후보만 남았다. 식별자 유출 후보는 실기기에서 값이 채워져야 성립하는데 지금은 검증할 방법이 없다. 그래서 이 앱은 파킹하기로 했다.

4주차부터 6주차까지 같은 APK를 파고든 결과 이 앱의 남은 임팩트는 실기기에 묶여 있다는 결론에 도달했다. 다음 스코프는 실기기가 필요 없는 타깃으로 다시 잡는다. 핵심은 같은 제조사 앱이라도 APK가 다르면 공격면과 종속성이 완전히 다르다는 점이다. 그래서 같은 계열이라도 표준 환경에서 소스부터 sink까지 끝까지 동적으로 증명되는 다른 APK로 옮기는 편이 낮다고 판단한다. 

새 타깃을 고를 때 먼저 볼 네 가지는 이렇다. 앱 진입 클래스가 패커 shell인지, 빠르게 확인되는 exported 이슈인지, 프로그램이 그 버그 클래스를 보상하는지, 외부 소스에서 민감 sink까지 짧고 동적으로 증명 가능한지다.
