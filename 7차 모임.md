6주차 앱은 임팩트가 벤더 실기기와 OS 종속 기능에 묶여 있어서 에뮬레이터 환경만으로는 끝까지 증명할 수 없었다. 그래서 이번 7주차는 같은 벤더 계열이지만 APK가 다른 앱으로 옮겼다. 핵심 조건은 OS 제약 없이 표준 에뮬레이터에서 외부 소스부터 민감 sink까지 끝까지 동적으로 증명되는 타깃이라는 점이었다.

## **목표**

안드로이드 벤더의 헬스케어 앱을 대상으로 실제 임팩트가 입증되는 취약점을 찾아 버그바운티에 제출하는 것이 목표였다. 6주차에서는 임팩트 확정에 실기기가 필요한 후보만 남아 파킹했다. 그래서 이번에는 OS 제약이 없고 표준 에뮬레이터에서 끝까지 동적으로 증명되는 취약점만 노렸다.

## **진행 과정**

1. 정적 분석으로 취약 패턴을 특정했다. 매니페스트에서 OAuth 콜백을 처리하는 액티비티가 외부에 노출돼 있고 커스텀 스킴으로 호출 가능한 것을 확인했다.
2. 콜백 처리 코드를 확인했다. 콜백 핸들러가 외부에서 들어온 인증 코드와 scope를 그대로 읽었다.
3. 상태 저장 시점의 문제도 확인했다. 토큰 교환 성공을 기다리지 않고 교환을 시작한 직후에 연결 상태를 저장하고 UI를 연결됨 상태로 바꿨다.
4. 본인 소유의 OAuth 테스트 계정으로 클라이언트를 승인했다. 승인 후 반환된 콜백에서 state 값이 비어 있는 것을 실제로 확인했다.
5. 외부 콜백 주입을 재현했다. 발급된 일회용 코드를 외부 Intent로 실행 중인 콜백 액티비티에 전달했다. 시스템이 해당 코드를 노출된 액티비티로 전달하는 것이 확인됐다.
6. 콜백 전달 후 앱 화면의 연동 버튼이 연결됨 상태로 바뀌었다. UIAutomator로 그 버튼이 실제 대상 앱의 UI 요소임을 명령줄에서 재확인했다.

## **사용 도구**

정적 분석 도구, 디바이스 제어 도구, 동적 후킹 도구, UI 검증 도구, 본인 소유의 OAuth 테스트 계정을 사용했다.
환경은 표준 Android 15 API 35 에뮬레이터다. 

## **성과**

이번에는 취약점을 확정하고 실제로 제출까지 완료했다.

취약점 클래스는 OAuth 연동 과정의 CSRF다. 근본 원인은 인증 요청에 state가 없고 노출된 콜백 액티비티가 외부 코드를 state 검증 없이 받아 토큰 교환으로 넘긴다는 점이다.

동적으로 입증한 범위는 외부 콜백 수락, 토큰 교환 경로 진입, 연결 상태 저장, UI 변경까지다. 6주차 앱과 달리 소스부터 상태 변화까지 표준 에뮬레이터에서 끝까지 재현했다.

제출 후 예비 검토를 통과했고 현재 보안팀 심사 단계에 있다.

## **회고**

이번 성공은 6주차에서 정한 기준이 그대로 통했다. 임팩트 확정에 실기기가 필요한 후보를 걸러내고 OS 제약 없이 표준 에뮬레이터에서 끝까지 증명되는 타깃만 골랐다. 그 결과 소스부터 sink까지 짧고 동적으로 이어지는 취약점을 잡았다. 같은 벤더라도 APK를 바꾸는 판단이 실제 제출로 이어졌다.

방법론 측면에서 세 가지를 지켰다. 첫째 노출된 컴포넌트라는 사실만이 아니라 state 미검증이라는 실제 논리 결함까지 짚었다. 둘째 본인 소유 계정만 써서 제3자 데이터에 손대지 않았다. 셋째 실제로 수행한 범위와 잠재적 영향을 분리해서 과장 없이 기술했다.

수정 권고는 명확하다. 인증 요청에 예측 불가능한 state를 생성해 세션과 묶고 콜백에서 이를 검증해야 한다. 연결 상태 저장도 토큰 교환이 성공적으로 끝난 뒤로 미뤄야 한다. 노출된 콜백은 단일 사용 state 없이는 처리하지 않아야 한다.
